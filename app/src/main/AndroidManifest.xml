<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dmitry.handheld_dictionary">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:name=".App"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.activity.MainActivity"
                  android:label="@string/app_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".ui.activity.GroupListActivity"/>

        <activity android:name=".ui.activity.OneGroupWordListActivity"/>

        <activity android:name=".ui.activity.WordEditActivity"
                  android:windowSoftInputMode="stateAlwaysHidden|adjustResize"/>

        <activity android:name=".ui.activity.GroupEditActivity"
                  android:windowSoftInputMode="stateAlwaysHidden|adjustResize"/>

        <activity android:name=".ui.activity.PagerActivity"
            android:label="@string/navigation_drawer_item_check"/>

        <provider
            android:name=".storage.AppContentProvider"
            android:authorities="com.example.dmitry.handheld_dictionary.storage.content_provider"
            android:exported="false"/>

    </application>

</manifest>
